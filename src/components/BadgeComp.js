import React from "react";
import { Link } from "react-router-dom";

const BadgeComp = () => {
    return (
        <div>
            BadgeComp
            <Link to="/study">공부하러 가자!</Link>
        </div>
    );
};

export default BadgeComp;
